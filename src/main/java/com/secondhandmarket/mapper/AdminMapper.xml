<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.secondhandmarket.mapper.AdminMapper">

    <select id="adminLogin" resultType="com.secondhandmarket.pojo.Admin" >
        select * from admin where phone=#{phone} and password=#{password}
    </select>

</mapper>